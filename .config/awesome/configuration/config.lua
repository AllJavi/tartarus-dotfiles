return {
	widget = {
		network = {
			-- Wired interface
			wired_interface = 'enp3s0',
			-- Wireless interface
			wireless_interface = 'wlan0'
		}
	},

	module = {
		auto_start = {
			-- Will create notification if true
			debug_mode = true
		}
	}
}
